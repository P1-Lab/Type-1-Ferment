Interface State Model (Mapped to Auream OS System Modes)

This document defines the operational screens of the touchscreen HMI as direct representations of system state machine modes.

1. Primary Navigation States

The interface is state-driven. Only one primary system mode is active at any time.

[BOOT]
  ↓
[IDLE / READY]
  ↓
[SELECT MODE]
  ↓
[ACTIVE OPERATION]
  ↓
[HOLD / MONITOR]
  ↓
[STERILIZATION]
  ↓
[FAULT / SAFE STATE]
